<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gyungdal.schooluniform_student">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".activity.SetSchool"
            android:label="@string/set_school"/>
        <activity android:name=".activity.signup.SignUp"
            android:label="@string/signup_page_title"/>

        <activity android:name=".activity.board.list.ThreadList"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AutoLoginSplash"
            android:label="@string/auto_login_page_title"/>

        <activity android:name=".activity.board.detail.SingleThread"
            android:label="test"/>
        <activity android:name=".activity.board.upload.UploadThread"
            android:label="@string/upload_page_title">
        </activity>

    </application>
</manifest>